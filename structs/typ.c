#include "main.h"
#include <stdio.h>

typedef struct students learner;

int main(void)
{
	learner pupil;

	age = 2;
	name = "Daisy Mumbo";
	grade = "95";

	printf("My name is %s, rollno %d scored %f\n", age, rollno, grade);
	return (0);
}
